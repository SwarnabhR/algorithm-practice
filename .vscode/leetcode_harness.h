// Mirrors the auto-runner harness (tools/runner.py) for IntelliSense only.
// This file is never compiled into any build; it exists so the C/C++
// extension can resolve LeetCode solution.cpp files, which intentionally
// contain no includes (the harness injects these at build time).
#include <bits/stdc++.h>
using namespace std;