// Scratch file for testing snippets, C++ features, and algorithm ideas.
// Not tied to any problem — edit freely. Run it with the "Run Tests" task
// (Ctrl+Alt+T) or just build.ps1 directly; it builds like any other
// single-file platform (see README.md).

#include <bits/stdc++.h>

using namespace std;

int main() {
    string s = "07:03:23AM";
    cout << s.substr(8);

    return 0;
}
